VITE_TILED_API_URL=http://127.0.0.1:8000/api/v1
VITE_TILED_API_KEY=secret
# Optional: facility dashboard YAML (overrides built-in default; Setup can still override in localStorage)
# VITE_DASHBOARD_URI=http://127.0.0.1:8000/schemas/lambda_mx_dashboard.yaml
# Optional: collection YAML that downselects the crate universe
# VITE_COLLECTION_URI=http://127.0.0.1:8000/schemas/lambda_mx_collection.yaml
